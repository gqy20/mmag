{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mmag.ai/skills/meeting/output/1.1.0",
  "x-version": "1.1.0",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "title", "summary", "decisions", "action_items", "open_questions",
    "participants", "message_range", "coverage_notes"
  ],
  "properties": {
    "title": {"type": "string", "minLength": 1},
    "summary": {"type": "string", "minLength": 1},
    "decisions": {"type": "array", "items": {"$ref": "#/$defs/sourced_item"}},
    "action_items": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["content", "owner_username", "due_text", "due_at", "source_post_ids"],
        "properties": {
          "content": {"type": "string", "minLength": 1},
          "owner_username": {"type": ["string", "null"]},
          "due_text": {
            "type": ["string", "null"],
            "description": "讨论中明确出现的截止时间原文；不得改写或规范化"
          },
          "due_at": {
            "type": ["string", "null"],
            "description": "可唯一确定时，由模型规范化的上海本地截止时间",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}T(?:[01]\\d|2[0-3]):[0-5]\\d:[0-5]\\d\\+08:00$"
          },
          "source_post_ids": {"$ref": "#/$defs/source_post_ids"}
        }
      }
    },
    "open_questions": {"type": "array", "items": {"$ref": "#/$defs/sourced_item"}},
    "participants": {
      "type": "array",
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1}
    },
    "message_range": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "message_count", "source_post_ids"],
      "properties": {
        "type": {"enum": ["thread", "recent", "since"]},
        "message_count": {"type": "integer", "minimum": 0},
        "source_post_ids": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        }
      }
    },
    "coverage_notes": {"type": "array", "items": {"type": "string"}}
  },
  "$defs": {
    "source_post_ids": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1}
    },
    "sourced_item": {
      "type": "object",
      "additionalProperties": false,
      "required": ["content", "source_post_ids"],
      "properties": {
        "content": {"type": "string", "minLength": 1},
        "source_post_ids": {"$ref": "#/$defs/source_post_ids"}
      }
    }
  }
}
