version: 1
cases:
  - id: selects-for-link
    input:
      intent: link
      goal: 分析 https://example.com
      parameters: {}
    expect:
      selected: true
      capability_subset: [analyze_link]

  - id: rejects-empty-goal
    input:
      intent: link
      goal: ""
      parameters: {}
    expect_error: INVALID_CONTRACT
