Prepare the requested project plan or status brief.

Goal:
{task_goal}

Parameters:
{parameters_json}

Context references:
{context_refs_json}

Artifact references:
{artifact_refs_json}

Use null for an unknown owner or due date. Put missing decisions in open_questions instead of
inventing them. Do not claim that a task was created in an external system.
